<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="opcion">
	 
	<select id="SQL_listaOpcionesXidPerfil" resultType="OpcionesBean" parameterType="String">
	    select opc.* from tb_opciones opc,tb_detalleperfil det 
		where opc.cod_opcion = det.cod_opcion and det.cod_perfil=#{codigo}  order by opc.orden_opcion				
	</select>
	 
	        
</mapper>