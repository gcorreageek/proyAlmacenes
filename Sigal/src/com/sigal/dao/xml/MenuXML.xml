<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="menu">
	<select id="SQL_listaMenu" resultType="MenuBean" parameterType="String">
	    call sp_listaMenu(#{cod_perfil})			
	</select>
	<select id="SQL_listaMenuOpc" resultType="MenuOpcBean" parameterType="String">
	select opc.* from tb_opciones opc,tb_detalleperfil det 
	where opc.cod_opcion = det.cod_opcion and det.cod_perfil=#{codigo} 			
	</select>
	
	
	
	
	<select id="SQL_listaMenuOpc1" resultType="MenuOpcBean" parameterType="DetallePerfilBean">
	    call sp_listaOpcionesMenu(#{cod_Menu},#{cod_Perfil})				
	</select>
	        
</mapper>